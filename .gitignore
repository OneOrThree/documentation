# Ditto API key lives here; never commit it.
.env
.env.*

# Working checkout of ion-design/ditto.site — its own git repo, ~1.8G with
# node_modules and clone run artifacts. Cloned on demand, not vendored:
#   gh repo clone ion-design/ditto.site
ditto.site/

# archify visual-check 증거물 — 검증 산출물이지 소스가 아니다
portfolio/diagrams/archify/*.visual-check.*
